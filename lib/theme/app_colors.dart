import 'package:flutter/material.dart';

class AppColors {
  // Your three main ruby-red shades
  static const Color rubyDark = Color(0xFF541616);
  static const Color rubyMid = Color(0xFF8A0303);
  static const Color rubyLight = Color(0xFFFF2A2A);

  // Standard dark mode background to complement the reds
  static const Color background = Color(0xFF121212);
  static const Color surface = Color(0xFF1E1E1E);
  
  // Text colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
}